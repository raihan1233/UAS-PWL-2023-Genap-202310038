import React from 'react'
import { Alert } from 'react-bootstrap';

const AlertErrorImages = () => {
    return (
        <Alert variant='info' >
            please fill with url image in this form input
        </Alert>
    )
}

export default AlertErrorImages;